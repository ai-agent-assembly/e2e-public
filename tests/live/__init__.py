"""Live-core integration fixtures.

This package builds and runs a real ``aa-gateway`` from the
``agent-assembly`` core monorepo so SDK ⇄ core behaviour can be
verified end-to-end against a live gRPC listener (AAASM-2976).
"""
